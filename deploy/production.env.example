POSTGRES_DB=webstorage
POSTGRES_USER=webstorage
# Generate once: openssl rand -hex 32. Keep the existing password on later deploys.
POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_PASSWORD
# HTTPS origin, no trailing slash, path or custom port.
PUBLIC_APP_URL=https://storage.example.com
SESSION_TTL_HOURS=12
